# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Environment / Secrets
.env
.env.local
.lora-training-info.json
.lora-model-url.txt

# Node
node_modules/
dist/

# Temp
tmp/
*.tmp

# Do NOT ignore generated/ — it's committed (auto-generated TypeScript)
# Do NOT ignore atlas-registry.json — it's committed (auto-generated manifest)
# Do NOT ignore poses/sm, poses/md, poses/lg — they're committed (auto-resized)

# Large model weights (use LFS or external storage)
*.safetensors
